{
  "name": "provider-keeper",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "provider:test": "npm run test -workspace=packages/provider-keeper",
    "provider:coverage": "npm run coverage -workspace=packages/provider-keeper",
    "provider:build": "npm run build -workspace=packages/provider-keeper",
    "ui:build": "npm run build -workspace=packages/test-app",
    "ui:start": "npm run start -workspace=packages/test-app"
  },
  "workspaces": [
    "packages/provider-keeper",
    "packages/test-app"
  ],
  "engineStrict": true,
  "engines": {
    "node": "^14.17.6",
    "npm": "^7.23.0"
  }
}
