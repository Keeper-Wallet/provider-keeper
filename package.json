{
  "name": "provider-keeper",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "provider:test": "yarn workspace @waves/provider-keeper test",
    "provider:coverage": "yarn workspace @waves/provider-keeper coverage",
    "provider:build": "yarn workspace @waves/provider-keeper build",
    "ui:build": "yarn workspace test-app build",
    "ui:start": "yarn workspace test-app start",
    "eslint": "eslint --ext .ts,.js --max-warnings 0 .",
    "prettier": "prettier . --check"
  },
  "workspaces": [
    "provider",
    "test-app"
  ]
}
